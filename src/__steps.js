/*
Auth Terminology
-----------------------
"Register" or "sign up" [first time create a user]
"sign in" or "log in" [already you have a use account created]
*/

/*
1. get form value
set form onSubmitHandler
set event parameter
use event.target.(akhane input a using name value ta likhte hobe then).value

*/ 